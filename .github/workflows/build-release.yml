name: Build & Release APK

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - run: chmod +x gradlew

      - name: Generate Keystore
        run: |
          keytool -genkeypair \
            -alias wazakar \
            -keyalg RSA -keysize 2048 \
            -validity 9125 \
            -keystore wazakar.jks \
            -storepass wazakar123 \
            -keypass wazakar123 \
            -dname "CN=Wazakar,O=Islamic,C=EG" \
            -noprompt

      - name: Build APK
        run: |
          ./gradlew assembleRelease \
            -Pandroid.injected.signing.store.file=${{ github.workspace }}/wazakar.jks \
            -Pandroid.injected.signing.store.password=wazakar123 \
            -Pandroid.injected.signing.key.alias=wazakar \
            -Pandroid.injected.signing.key.password=wazakar123

      - name: Create Release with APK
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "build-${{ github.run_number }}"
          name: "وذكر - إصدار ${{ github.run_number }}"
          files: app/build/outputs/apk/release/*.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
